extends base

block content
  .home-container 
    .list-container
      .list-header
        h1 Products
        //- .search-box 
        //-   input#searchInput.form-input.search-input(type="text" placeholder="eg. category=emergency tolley")
        //-   input.btn.btn-search(type="submit" value="search")
          
      if products.length > 0
        each product in products
          include components/_productCard
            //- .button-box
            //-   a.btn-detail(href="#") detail
        
      if products.length === 0
        .no-more-product-container
          include components/_noMoreProduct
      include components/_paginator
    //- include components/_product