div.header
  div.logo
  div.menu
    ul 
      if user 
        li
          a.menu_link(href="#") #{user.name}
      li
        a.menu__link(href="/info") .info
      if user
        li 
          a.menu_link.signOut .logout